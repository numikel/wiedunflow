# Release-gate eval config for psf/requests.
# src-layout repo (src/requests/) — Jedi fix in jedi_resolver.py handles path.
llm:
  provider: "openai"
  model_plan: "gpt-5.4-mini"
  model_narrate: "gpt-5.4"
max_lessons: 30
target_audience: "mid-level Python developer"
exclude_patterns:
  - "tests/**"
