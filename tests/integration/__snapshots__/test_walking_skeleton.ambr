# serializer version: 1
# name: test_tutorial_html_snapshot
  '''
  <!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>tiny_repo — CodeGuide Tutorial</title>
    <style>
      /* Minimal walking skeleton styles — pixel-perfect design comes in Sprint 5 */
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
      body { font-family: system-ui, sans-serif; display: flex; height: 100vh; overflow: hidden; color: #1a1a1a; background: #fff; }
      #sidebar { width: 220px; min-width: 220px; border-right: 1px solid #e0e0e0; overflow-y: auto; padding: 16px 0; background: #fafafa; }
      #sidebar h2 { font-size: 12px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; color: #666; padding: 0 16px 8px; }
      #sidebar ul { list-style: none; }
      #sidebar li button { display: block; width: 100%; text-align: left; padding: 8px 16px; font-size: 14px; border: none; background: none; cursor: pointer; color: #333; }
      #sidebar li button:hover, #sidebar li button.active { background: #e8e8e8; font-weight: 600; }
      #main { flex: 1; display: flex; flex-direction: column; overflow: hidden; }
      #lesson-content { flex: 1; overflow-y: auto; padding: 32px 40px; max-width: 800px; }
      #lesson-content h1 { font-size: 24px; margin-bottom: 16px; }
      #lesson-content p { line-height: 1.7; margin-bottom: 12px; }
      #nav { display: flex; gap: 8px; padding: 12px 40px; border-top: 1px solid #e0e0e0; }
      #nav button { padding: 6px 16px; border: 1px solid #ccc; border-radius: 4px; cursor: pointer; background: #fff; font-size: 14px; }
      #nav button:disabled { opacity: 0.4; cursor: default; }
      #footer { text-align: center; font-size: 11px; color: #999; padding: 8px; border-top: 1px solid #e0e0e0; }
      .code-block { background: #f5f5f5; border: 1px solid #ddd; border-radius: 4px; padding: 12px; overflow-x: auto; font-size: 13px; margin: 12px 0; }
      .codeguide-banner { padding: 8px 12px; margin: 12px 0; border-radius: 4px; font-size: 0.9em; }
      .codeguide-banner--warn { background: #ffe8b3; color: #a15c00; }
      .codeguide-banner--info { background: #e3f0ff; color: #245a9e; }
    </style>
  </head>
  <body>
    <aside id="sidebar">
      <h2>Lessons</h2>
      <ul id="lesson-list"></ul>
    </aside>
    <div id="main">
      <div id="lesson-content">
        <h1 id="lesson-title"></h1>
        <div id="lesson-narrative"></div>
      </div>
      <nav id="nav">
        <button id="btn-prev" class="prev" disabled>&#8592; Previous</button>
        <button id="btn-next" class="next">Next &#8594;</button>
      </nav>
      <footer id="footer">
        
        <div class="codeguide-banner codeguide-banner--warn" role="status">
          &#9888; Documentation coverage is low (0%).
          Some lessons may reference symbols with limited context.
        </div>
        
        
        Generated by <strong>CodeGuide 0.0.3</strong>
        from <code>NORMALIZED_BRANCH@HASH</code>
        at 2026-01-01T12:00:00Z
        &nbsp;&middot;&nbsp; Offline &mdash; no external resources
      </footer>
    </div>
  
    <script type="application/json" id="tutorial-data">{"schema_version": "1.0.0", "repo_name": "tiny_repo", "lessons": [{"id": "lesson-001", "title": "The add function", "narrative": "## The add function\n\nIn this lesson, we explore the following: How to implement basic addition as a typed Python function.\n\nThis is a deterministic stub narrative generated by FakeLLMProvider. In production, this text is generated by a real LLM (Anthropic Claude). The stub exists so the full pipeline can be validated end-to-end without API keys.\n\nThe implementation follows a straightforward pattern: accept typed parameters, perform the operation, and return a typed result. Python's type hints make this code self-documenting at the function signature level.", "narrative_html": "<p>## The add function</p><p>In this lesson, we explore the following: How to implement basic addition as a typed Python function.</p><p>This is a deterministic stub narrative generated by FakeLLMProvider. In production, this text is generated by a real LLM (Anthropic Claude). The stub exists so the full pipeline can be validated end-to-end without API keys.</p><p>The implementation follows a straightforward pattern: accept typed parameters, perform the operation, and return a typed result. Python's type hints make this code self-documenting at the function signature level.</p>", "code_refs": ["calculator.add"], "status": "generated"}, {"id": "lesson-002", "title": "The subtract function", "narrative": "## The subtract function\n\nIn this lesson, we explore the following: How to implement basic subtraction and reuse the same pattern.\n\nThis is a deterministic stub narrative generated by FakeLLMProvider. In production, this text is generated by a real LLM (Anthropic Claude). The stub exists so the full pipeline can be validated end-to-end without API keys.\n\nThe implementation follows a straightforward pattern: accept typed parameters, perform the operation, and return a typed result. Python's type hints make this code self-documenting at the function signature level.", "narrative_html": "<p>## The subtract function</p><p>In this lesson, we explore the following: How to implement basic subtraction and reuse the same pattern.</p><p>This is a deterministic stub narrative generated by FakeLLMProvider. In production, this text is generated by a real LLM (Anthropic Claude). The stub exists so the full pipeline can be validated end-to-end without API keys.</p><p>The implementation follows a straightforward pattern: accept typed parameters, perform the operation, and return a typed result. Python's type hints make this code self-documenting at the function signature level.</p>", "code_refs": ["calculator.subtract"], "status": "generated"}, {"id": "lesson-003", "title": "CLI entry point", "narrative": "## CLI entry point\n\nIn this lesson, we explore the following: How the main() function ties the calculator together.\n\nThis is a deterministic stub narrative generated by FakeLLMProvider. In production, this text is generated by a real LLM (Anthropic Claude). The stub exists so the full pipeline can be validated end-to-end without API keys.\n\nThe implementation follows a straightforward pattern: accept typed parameters, perform the operation, and return a typed result. Python's type hints make this code self-documenting at the function signature level.", "narrative_html": "<p>## CLI entry point</p><p>In this lesson, we explore the following: How the main() function ties the calculator together.</p><p>This is a deterministic stub narrative generated by FakeLLMProvider. In production, this text is generated by a real LLM (Anthropic Claude). The stub exists so the full pipeline can be validated end-to-end without API keys.</p><p>The implementation follows a straightforward pattern: accept typed parameters, perform the operation, and return a typed result. Python's type hints make this code self-documenting at the function signature level.</p>", "code_refs": ["main.cli"], "status": "generated"}]}</script>
  
    <script>
      (function() {
        var raw = document.getElementById('tutorial-data').textContent;
        var data = JSON.parse(raw);
        var lessons = data.lessons;
        var current = 0;
  
        var listEl = document.getElementById('lesson-list');
        var titleEl = document.getElementById('lesson-title');
        var narrativeEl = document.getElementById('lesson-narrative');
        var btnPrev = document.getElementById('btn-prev');
        var btnNext = document.getElementById('btn-next');
  
        function renderLesson(idx) {
          var lesson = lessons[idx];
          titleEl.textContent = lesson.title;
          narrativeEl.innerHTML = lesson.narrative_html || lesson.narrative.replace(/\n/g, '<br>');
          btnPrev.disabled = idx === 0;
          btnNext.disabled = idx === lessons.length - 1;
          listEl.querySelectorAll('button').forEach(function(btn, i) {
            btn.classList.toggle('active', i === idx);
          });
          current = idx;
          try { localStorage.setItem('codeguide:last-lesson', String(idx)); } catch(e) {}
        }
  
        lessons.forEach(function(lesson, idx) {
          var li = document.createElement('li');
          var btn = document.createElement('button');
          btn.textContent = (idx + 1) + '. ' + lesson.title;
          btn.addEventListener('click', function() { renderLesson(idx); });
          li.appendChild(btn);
          listEl.appendChild(li);
        });
  
        btnPrev.addEventListener('click', function() { if (current > 0) renderLesson(current - 1); });
        btnNext.addEventListener('click', function() { if (current < lessons.length - 1) renderLesson(current + 1); });
  
        var saved = 0;
        try { saved = parseInt(localStorage.getItem('codeguide:last-lesson') || '0', 10) || 0; } catch(e) {}
        renderLesson(Math.min(saved, lessons.length - 1));
      })();
    </script>
  </body>
  </html>
  '''
# ---
